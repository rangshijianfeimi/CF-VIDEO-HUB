正式版 **v2.3.1**，Docker 镜像 `ghcr.io/fe-spark/ecohub:v2.3.1` 与 `ghcr.io/fe-spark/ecohub:latest`。

### 升级指引

- **从 v2.1.x / v2.2.x / v2.3.0 升级**：执行 `docker compose pull && docker compose up -d` 即可（或后台「检查更新」一键平滑升级）。
- **从 v2.1.0 之前升级**：请先按 [v2.1.0 说明](https://github.com/fe-spark/EcoHub/releases/tag/v2.1.0) 完成素材卷目录迁移，再升级到本版本。

---

### v2.3.1 核心变更

#### 1. 片库检索筛选面板深度重构
- **多端组件化拆分**：将片库检索筛选面板独立拆分为桌面端胶囊面板（`DesktopFilterPanel`）与移动端底部抽屉面板（`MobileFilterDrawer`），大幅优化响应式体验与代码维护性。
- **已选条件与快捷操作**：新增已选筛选标签快捷栏，支持单个标签即时删除、一键清空重置与条件项实时计数回显。
- **移动端交互调优**：顶部支持一级分类横滑快速切换，底部抽屉提供全维度标签勾选与确定后即时过滤。

#### 2. 播放详情页信息卡片重构
- **视觉排版升级**：重构播放页顶部信息卡片布局，引入独立高亮综合评分徽章（`scoreBadge`）与状态胶囊标签。
- **元数据与主演信息流**：优化更新时间、分类、地区、年代元数据标签排版，并新增主演芯片展示行（`actorRow`），移动端自适应折行。

#### 3. 全局 Header 与播放器体验优化
- **移动端 Header 增强**：统一移动端固定定位与背景隔离，提升移动端导航抽屉与历史记录面板层级，杜绝内容穿透与遮挡。
- **播放器行内与全屏优化**：禁用 Artplayer 全屏 backdrop 遮罩，消除移动端全屏变暗及拦截 Header 点击的问题；完善 H5 / 微信 / 移动端行内播放标签属性（`playsinline`、`x5-playsinline`）。

#### 4. 鸿蒙客户端升级 (v1.1.1)
- **子模块同步升级**：同步 `app-for-ohos` 至 v1.1.1 正式版本。
- **回到顶部体验统一**：全量统一鸿蒙端各个 Tab 页面在滚动超出一屏时展示平滑「回到顶部」浮动按钮。
