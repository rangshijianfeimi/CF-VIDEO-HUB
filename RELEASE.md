正式版 **v2.2.0**，Docker 镜像 `ghcr.io/fe-spark/ecohub:v2.2.0` 与 `ghcr.io/fe-spark/ecohub:latest`。

### 升级指引

- **从 v2.1.x 升级**：执行 `docker compose pull && docker compose up -d` 即可（或后台「检查更新」一键平滑升级）。
- **从 v2.1.0 之前升级**：请先按 [v2.1.0 说明](https://github.com/fe-spark/EcoHub/releases/tag/v2.1.0) 完成素材卷目录迁移，再升级到本版本。

---

### v2.2.0 核心变更

#### 1. 核心性能与查询架构重构（突破高并发与海量数据瓶颈）
- **全内存极速片名搜索索引**：构建 `filmSearchMemoryIndex` 内存索引（20w 条常驻内存仅占 ~16MB），前台搜片与 TVBox/MacCMS 搜片全面接入，搜索耗时从数秒级直降至 **1~2ms**。
- **每日更新架构读写分离与零查库抽取**：重构每日更新候选池机制，采集写路径自动补齐至 120 部，读请求纯 Redis 内存抽取并随机采样，彻底消除 MySQL 临时表全量排序与重复查库，耗时从 4.57s 降至 **< 0.2ms**。
- **轻量读模型与 OOM 根治**：重构 GORM `FindInBatches` 批量读取与分批加载，精简读模型内存分配，彻底根治大规模影片数据下的内存暴涨与 OOM 问题。
- **相关推荐候选内存极速召回**：优化相关推荐候选集召回算法，优先内存秒级匹配，彻底移除 `sub_title` 等 TEXT 大字段慢表扫描，推荐耗时从 1.43s 降至 **< 5ms**。
- **快照状态与活跃分类树索引直查**：重构 `LiveUpdateRemarksByMIDs` 与分类树构建，消除大 JSON 反序列化与全表 DISTINCT 扫描，首页加载由 10.5s 降至 **秒开**。
- **Redis 批量删除优化**：优化缓存清理为每 100 个 Key 批量并发删除，降低 Redis 单线程阻塞风险。

#### 2. 采集系统稳定性与自适应防御
- **429 限流自适应防御**：针对外部源 429 Too Many Requests 等限流场景，实现指数退避与自适应平滑重试。
- **定时任务与集数上榜机制优化**：优化采集判定，仅在全库首次抬高影片集数时记录为最新更新上榜；修复定时失败时错误解开同站 suppress 的问题。
- **分类标签快照完整性自愈**：增加筛选标签快照校验机制，启动自动补全与自愈修复残缺脏数据。

#### 3. 多端客户端生态与体验全面升级
- **OpenHarmony / HarmonyOS 原生客户端（EcoHub for OHOS v1.0.3）**：
  - 新增搜索历史持久化管理，支持多软件源独立隔离与 LRU 自动维护；
  - 搜索页新增历史搜索流式标签（Chips），支持快捷回填、单条删除与全部清空；
  - 搜索框增加一键清空按钮，清空即重置回历史视图；
  - 适配独立流式每日更新与多源切换。
- **Android 客户端（EcoHub for Android）**：
  - 作为 submodule 规范化集成，完善多源接入与影视播放能力。

